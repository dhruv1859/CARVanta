[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["python3 -m pip install -r requirements-render.txt"]

[start]
cmd = "python3 -m uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8001} --workers 1"
