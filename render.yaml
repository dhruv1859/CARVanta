# ─── CARVanta Backend — Render Blueprint ────────────────────────────────────
# NOTE: CARVanta is deployed on Railway. This file is kept for reference only.
# Railway uses nixpacks.toml + railway.json instead.
# services:
#   - type: web
#     name: carvanta-api
#     runtime: python
#     buildCommand: pip install -r requirements-render.txt
#     startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
#     envVars:
#       - key: PYTHON_VERSION
#         value: "3.12.0"
#       - key: CARVANTA_ENV
#         value: production
#     plan: free
